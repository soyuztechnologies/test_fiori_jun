# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: fiorilikeapp
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: "ANUBHAV"
          url: http://122.162.240.164:8010
          client: "800"
        app:
          name: ZAB_JUN_FIORI
          description: my fiori fiori app live
          package: Z_FANA
          transport: S10K900775
        exclude:
          - /test/
